spring:
  application:
    name: fams-api-server

  datasource:
    url: jdbc:postgresql://localhost:5433/fams_db
    username: fams_user
    password: anhanh0505

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080