fastapi==0.111.0
uvicorn[standard]==0.29.0
deepface==0.0.93
# Face detection (SCRFD) + recognition (ArcFace) + 106/68-point landmarks + head pose, replacing
# the old dlib-based face_recognition pipeline entirely (2026-07-31) — see
# docs/api/face-id-management-api.md for why (dlib 6-point + solvePnP had a systematic pose bias
# that real-device testing exposed; ArcFace embeddings are also meaningfully more discriminative
# than dlib's ResNet-34 ones for identity verification). --no-deps-installed transitively at
# build time; pinned exactly here (not left to the resolver) because this environment's PyPI
# access is bandwidth-constrained and unpinned resolution can spend a very long time searching
# version combinations — see git history / face-id-management-api.md 0.0b for how that was found.
insightface==1.0.1
onnxruntime==1.28.0
onnx==1.22.0
scipy==1.17.1
scikit-image==0.26.0
networkx
imageio
tifffile
lazy_loader
PyWavelets
tqdm
requests
numpy==1.26.4
Pillow==10.3.0
# opencv-python-headless conflicts with deepface's opencv-python dependency.
# deepface manages opencv; libglib2.0-0/libsm6/libxext6 in Dockerfile provide runtime libs.
# insightface also needs cv2 — the same opencv-python install satisfies it, no separate
# opencv-contrib-python needed (verified: insightface only uses core cv2 APIs already present).
psycopg2-binary==2.9.9
redis==5.0.4
httpx==0.27.0
pydantic==2.7.1
python-multipart==0.0.9
python-dotenv==1.0.1
